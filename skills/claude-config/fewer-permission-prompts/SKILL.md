---
name: fewer-permission-prompts
description: 减少权限提示 — 扫描转录记录并添加 allowlist
category: claude-config
triggers: [permission, 权限, allowlist, 允许, 减少提示, 自动允许]
---

# fewer-permission-prompts

减少权限提示 — 扫描转录记录并添加 allowlist

## 概述

扫描转录记录中的常见只读 Bash 和 MCP 工具调用，添加优先 allowlist 以减少权限提示。

## 使用场景

触发关键词: permission, 权限, allowlist, 允许, 减少提示, 自动允许

## 分类

所属分类: `claude-config`

## 注意事项

本 Skill 由 Claude Code 自动加载。使用 Skill 工具调用。
