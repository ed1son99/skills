---
name: update-config
description: Claude Code 配置管理 — settings.json 和 hooks 配置
category: claude-config
triggers: [settings, config, 配置, hook, permission, 权限, env, 环境变量, settings.json]
---

# update-config

Claude Code 配置管理 — settings.json 和 hooks 配置

## 概述

通过 settings.json 配置 Claude Code。自动化行为需要 hooks 配置。也适用于权限管理、环境变量设置、hook 故障排除。

## 使用场景

触发关键词: settings, config, 配置, hook, permission, 权限, env, 环境变量, settings.json

## 分类

所属分类: `claude-config`

## 注意事项

本 Skill 由 Claude Code 自动加载。使用 Skill 工具调用。
