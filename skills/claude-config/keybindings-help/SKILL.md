---
name: keybindings-help
description: 键盘快捷键自定义 — ~/.claude/keybindings.json 配置
category: claude-config
triggers: [keybinding, 快捷键, 键盘, shortcut, rebind, 绑定]
---

# keybindings-help

键盘快捷键自定义 — ~/.claude/keybindings.json 配置

## 概述

自定义键盘快捷键、重新绑定按键、添加组合键。修改 ~/.claude/keybindings.json。

## 使用场景

触发关键词: keybinding, 快捷键, 键盘, shortcut, rebind, 绑定

## 分类

所属分类: `claude-config`

## 注意事项

本 Skill 由 Claude Code 自动加载。使用 Skill 工具调用。
