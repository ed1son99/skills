# claude-config

Skills in the claude-config category:

- **update-config** — Claude Code 配置管理 — settings.json 和 hooks 配置
- **keybindings-help** — 键盘快捷键自定义 — ~/.claude/keybindings.json 配置
- **claude-api** — Claude API 参考 — 模型 ID、定价、参数、流式、工具使用、MCP
- **fewer-permission-prompts** — 减少权限提示 — 扫描转录记录并添加 allowlist
- **using-agent-skills** — Agent Skill 发现与调用 — 元技能，管理所有其他 skill 的发现和调用
