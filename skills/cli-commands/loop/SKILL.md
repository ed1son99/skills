---
name: loop
description: CLI 循环 — 按间隔重复执行 prompt 或 slash command
category: cli-commands
triggers: [/loop, 循环, 重复, poll, 定时, 监控]
---

# loop

CLI 循环 — 按间隔重复执行 prompt 或 slash command

## 概述

按定期间隔运行 prompt 或 slash command。省略间隔让模型自定节奏。适用于设置定期任务或状态轮询。

## 使用场景

触发关键词: /loop, 循环, 重复, poll, 定时, 监控

## 分类

所属分类: `cli-commands`

## 注意事项

本 Skill 由 Claude Code 自动加载。使用 Skill 工具调用。
